package cl.folletos.controlador;

// AuthControlador intentionally left non-controller to avoid duplicate /login mapping
public class AuthControlador {
    // previously provided a /login mapping; removed to avoid conflict with existing LoginControlador
}